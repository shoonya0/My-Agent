# MyAgent Infrastructure Services
# Provides: Redis, Kafka, MySQL, Jaeger, MinIO
#
# Usage (from repo root):
#   docker compose up -d
#   docker compose ps
#   docker compose logs -f
#
# Stop services:
#   docker compose down
#
# See docker/README.md for detailed documentation.

include:
  - docker/docker-compose.yml
